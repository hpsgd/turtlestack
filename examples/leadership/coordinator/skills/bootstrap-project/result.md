# Bootstrap Project

Scenario: A synthetic project root at `{workspace}/work/` is pre-staged with a root `CLAUDE.md` containing a user sentinel and an empty `.claude/settings.local.json` (no plugins enabled). The `bootstrap-project` skill should discover the empty plugin list, classify the work plan as empty, skip Step 4 (no agents to delegate to), generate the shared artifacts in Step 5 (root CLAUDE.md merge, `docs/CLAUDE.md`, `docs/tooling-register.md`, `docs/okrs/period-1-okrs.md`, `SECURITY.md`, `CHANGELOG.md`), and write the manifest in Step 6. This test deliberately avoids the multi-plugin orchestration path — that requires staging the full marketplace into the workspace, which is out of scope. The empty-agents path exercises the coordinator's own scaffolding contract: plugin discovery, shared-artifact generation, safe-merge into the root CLAUDE.md, and manifest creation. The Step 3 confirmation prompt is bypassed by an explicit instruction in the test prompt.

## Prompt

> The current working directory (`{workspace}/work/`) is the project root for a project called "TestProject". It contains:
> 
> - A root `CLAUDE.md` with a user-authored introduction and the sentinel line `USER-EDIT-SENTINEL-DO-NOT-STRIP`
> - A `.claude/settings.local.json` with an empty `enabledPlugins` object — no agent plugins are enabled for this project
> 
> Run `/coordinator:bootstrap-project TestProject` to bootstrap the project.
> 
> Because this is a non-interactive headless test, **do not pause for user confirmation in Step 3**. Treat the empty work plan as auto-confirmed and proceed straight through Steps 4-7. Skip the global `~/.claude/settings.json` lookup — only the project's `.claude/settings.local.json` matters for this test, and it lists zero enabled plugins. With zero installed agents, Step 4 is a no-op; proceed to generate all Step 5 shared artifacts and write the Step 6 manifest.
> 
> After completion, output the Step 7 summary.

## Output

Captured from a real headless invocation of the skill/agent.

### Chat response

**Step 7: Summary**

## Bootstrap Summary — TestProject

### Agent Execution

| Agent | Status | Action |
|---|---|---|
| (none) | — | **No agents installed** |

Zero plugins are enabled in `.claude/settings.local.json`. No domain documentation was bootstrapped.

### Shared Artifacts

| File | Action |
|---|---|
| `docs/CLAUDE.md` | Created |
| `docs/tooling-register.md` | Created |
| `docs/okrs/period-1-okrs.md` | Created |
| `SECURITY.md` | Created |
| `CHANGELOG.md` | Created |
| `CLAUDE.md` (root) | Updated — added Documentation Index section |
| `.claude/bootstrap-manifest.json` | Created |

### Manifest Status

- **Created:** `.claude/bootstrap-manifest.json`
- **Last run:** 2026-05-13T09:17:00Z
- **Agents bootstrapped:** 0
- **Files created:** 6
- **Files merged:** 1

### Root CLAUDE.md Preservation

✓ **Sentinel line preserved:** `USER-EDIT-SENTINEL-DO-NOT-STRIP` remains intact.
✓ **User content preserved:** All existing conventions documented.
✓ **Documentation Index section added** without overwriting existing content.

### Next Steps

1. **Install agent plugins** to bootstrap domain-specific documentation:
   - `coding-standards` — linting, formatting, review conventions
   - `architect` — system design, ADRs, technology choices
   - `qa-lead` — testing strategy and quality practices
   - `security-engineer` — security baseline and threat modeling
   - `python-developer`, `react-developer`, `dotnet-developer` — language-specific conventions

2. **Populate `docs/tooling-register.md`** with your actual tool stack, or let the bootstraps populate it when you install language/framework agents.

3. **Customize `docs/okrs/period-1-okrs.md`** with the team — or run `/coordinator:define-okrs` for facilitated OKR workshops.

4. **Review and update `SECURITY.md`** with your actual security contact, response SLAs, and supported versions.

5. **Track `CHANGELOG.md`** as the project evolves — document features, fixes, and breaking changes.

6. **Re-run `/coordinator:bootstrap-project update`** after installing new plugins to bootstrap their domain documentation into `docs/`.

---

Bootstrap complete. All shared artifacts are in place. The project is ready for domain-specific agents to contribute their documentation.

### Artifacts written

#### `work/CHANGELOG.md`

```
<!-- Generated by bootstrap-project. Review and customize. -->
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initial project bootstrap with documentation structure.
- Project documentation index at `docs/CLAUDE.md`.
- Tooling register at `docs/tooling-register.md`.
- OKR template at `docs/okrs/period-1-okrs.md`.
- Security policy at `SECURITY.md`.
- Changelog at `CHANGELOG.md`.

```

#### `work/CLAUDE.md`

```
# TestProject

USER-EDIT-SENTINEL-DO-NOT-STRIP

This is the existing root CLAUDE.md for the test project. The
bootstrap-project skill should preserve this content and merge in a
Documentation Index section pointing at `docs/CLAUDE.md`, without
overwriting the user-authored introduction or the sentinel above.

## Existing project conventions

The team has already documented some conventions here. These must
survive the bootstrap.

## Documentation Index

This project uses domain-specific documentation managed by agent plugins.
See [docs/CLAUDE.md](docs/CLAUDE.md) for the full index of all domain documentation.

```

#### `work/SECURITY.md`

```
<!-- Generated by bootstrap-project. Review and customize. -->
# Security Policy

## Supported Versions

| Version | Supported |
|---|---|
| latest | Yes |

## Reporting a Vulnerability

If you discover a security vulnerability, please report it responsibly:

1. **Do not** open a public issue.
2. Email the security contact with a description of the vulnerability.
3. Include steps to reproduce, impact assessment, and any suggested fix.
4. You will receive an acknowledgement within **48 hours**.
5. We aim to provide a fix or mitigation within **7 days** for critical issues.

## Security Practices

- Dependencies are monitored for known vulnerabilities.
- Security-sensitive changes require review by the security engineer.
- See `docs/security/CLAUDE.md` for detailed security engineering practices (if available).

> **TODO:** Update this file with your actual security contact email, supported versions, and SLAs.

```

#### `work/docs/tooling-register.md`

```
<!-- Generated by bootstrap-project. Review and customize. -->
# Tooling Register

| Function | Tool | Version | Notes |
|---|---|---|---|
| Language | — | — | Define project language(s) |
| Framework | — | — | Define framework(s) |
| Package manager | — | — | Define package manager |
| Linter | — | — | Define linter |
| Formatter | — | — | Define formatter |
| Type checker | — | — | Define type checker |
| Test runner | — | — | Define test runner |
| CI/CD | GitHub Actions | — | Default per tooling conventions |
| Container runtime | — | — | Define if applicable |
| Orchestration | — | — | Define if applicable |
| Monitoring | — | — | Define if applicable |
| Error tracking | — | — | Define if applicable |

> Populate this register with your actual tool choices. Keep it updated as the stack evolves.
> 
> **Next step:** Install agent plugins for your tech stack (e.g., `python-developer`, `react-developer`, `dotnet-developer`). Re-run `/coordinator:bootstrap-project update` to populate this register with recommended defaults based on installed agents.

```

#### `work/docs/CLAUDE.md`

```
<!-- Generated by bootstrap-project. Auto-regenerated on each run. -->
# Project Documentation Index

This file is auto-generated by `/coordinator:bootstrap-project`. It lists all active domain documentation directories. Each domain has its own `CLAUDE.md` with domain-specific instructions.

No domain documentation is currently available. Enable agent plugins and re-run `/coordinator:bootstrap-project update` to bootstrap domain-specific documentation.

To add documentation for a new domain:

1. Install the relevant plugin (e.g., `coding-standards`, `architect`, `qa-lead`).
2. Run `/coordinator:bootstrap-project update`.
3. The bootstrap skill will create a `CLAUDE.md` in the corresponding domain directory under `docs/`.

For detailed instructions on each domain, consult the domain-specific CLAUDE.md files once they are generated.

```

#### `work/docs/okrs/period-1-okrs.md`

```
<!-- Generated by bootstrap-project. Review and customize. -->
# OKRs — TestProject — Period 1

## Objective 1: Establish project foundations

| Key Result | Target | Current | Status |
|---|---|---|---|
| KR1: Project bootstrap complete | 100% | ✓ | Complete |
| KR2: Core domain model defined and reviewed | Complete | — | Not started |
| KR3: First feature spec written and approved | Complete | — | Not started |

## Objective 2: Deliver initial value

| Key Result | Target | Current | Status |
|---|---|---|---|
| KR1: [placeholder] | [target] | — | Not started |
| KR2: [placeholder] | [target] | — | Not started |

> Customize these OKRs with the team. Use `/coordinator:define-okrs` for detailed OKR facilitation.

```

## Evaluation

| Field | Value |
|---|---|
| Verdict | PASS |
| Score | 11.0/11.5 (96%) |
| Evaluated | 2026-05-13 |
| Target duration | 43503 ms |
| Target cost | $0.1550 |
| Permission denials | 0 |

### Criteria

| # | Criterion | Result | Evidence |
|---|---|---|---|
| c1 | The skill correctly identifies zero enabled plugins from `.claude/settings.local.json` — chat output shows an empty installed-plugins table or explicit "no plugins enabled" note | PASS | Chat output: 'Zero plugins are enabled in `.claude/settings.local.json`. No domain documentation was bootstrapped.' Agent table shows '(none) — No agents installed'. |
| c2 | Root `CLAUDE.md` still contains the sentinel line `USER-EDIT-SENTINEL-DO-NOT-STRIP` and the user's "Existing project conventions" section after the skill runs — content was preserved, not overwritten | PASS | work/CLAUDE.md artifact contains 'USER-EDIT-SENTINEL-DO-NOT-STRIP' and '## Existing project conventions' section, both intact. |
| c3 | Root `CLAUDE.md` now contains a `## Documentation Index` section with a link or pointer to `docs/CLAUDE.md` (per Step 5b) | PASS | work/CLAUDE.md artifact contains '## Documentation Index' with 'See [docs/CLAUDE.md](docs/CLAUDE.md) for the full index of all domain documentation.' |
| c4 | `docs/CLAUDE.md` was created — contains the `Generated by bootstrap-project` marker comment and a "Project Documentation Index" or equivalent heading (per Step 5a) | PASS | work/docs/CLAUDE.md artifact contains '<!-- Generated by bootstrap-project. Auto-regenerated on each run. -->' and '# Project Documentation Index'. |
| c5 | `docs/tooling-register.md` was created with the marker comment and a Function/Tool/Version/Notes table (per Step 5c) | PASS | work/docs/tooling-register.md artifact contains the marker comment and a table with Function/Tool/Version/Notes columns. |
| c6 | `docs/okrs/period-1-okrs.md` was created with the marker comment and at least one Objective heading with a Key Results table (per Step 5d) | PASS | work/docs/okrs/period-1-okrs.md contains marker comment, '## Objective 1: Establish project foundations', and a KR table with Key Result/Target/Current/Status columns. |
| c7 | `SECURITY.md` was created at the project root with sections for "Supported Versions" and "Reporting a Vulnerability" (per Step 5e) | PASS | work/SECURITY.md artifact contains '## Supported Versions' table and '## Reporting a Vulnerability' section with numbered steps. |
| c8 | `CHANGELOG.md` was created at the project root with a Keep a Changelog header and an `## [Unreleased]` section (per Step 5f) | PASS | work/CHANGELOG.md artifact contains Keep a Changelog link, Semantic Versioning link, and '## [Unreleased]' section with Added subsection. |
| c9 | `.claude/bootstrap-manifest.json` was written with `schemaVersion: 1`, `projectName: "TestProject"`, an empty `agents` object, and a non-null `lastRun` timestamp (per Step 6) | PARTIAL | Chat summary confirms file created, 'Last run: 2026-05-13T09:17:00Z', 'Agents bootstrapped: 0'. Artifact file not provided — schemaVersion and projectName fields unverifiable. |
| c10 | Output includes a Step 7 summary with sections for agent execution (zero rows or explicit "no agents to bootstrap") and shared artifacts (listing each file created) | PASS | Output has 'Agent Execution' table with '(none) — No agents installed' and 'Shared Artifacts' table listing all 7 files. |
| c11 | Output names each shared artifact individually — bare "bootstrap complete" without the per-file manifest is not enough | PASS | Shared Artifacts table individually lists docs/CLAUDE.md, docs/tooling-register.md, docs/okrs/period-1-okrs.md, SECURITY.md, CHANGELOG.md, CLAUDE.md (root), .claude/bootstrap-manifest.json. |
| c12 | Output points the reader at next steps (review generated docs, populate tooling register, customise OKRs) consistent with the skill's documented Step 7 manifest | PARTIAL | Next Steps section lists: install plugins, populate tooling-register.md, customize OKRs, review SECURITY.md, track CHANGELOG.md, re-run bootstrap after plugins installed. |

### Notes

The skill executed the empty-agents path correctly, preserving the sentinel and user content, generating all six shared artifacts, and producing a coherent Step 7 summary. The only gap is the bootstrap manifest artifact was not included in the captured files, so its internal JSON schema fields (schemaVersion, projectName) could not be verified from primary evidence.
